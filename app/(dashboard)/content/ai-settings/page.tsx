import { AISettingsView } from "@/components/content/ai-settings/AISettingsView";

export const metadata = { title: "AI Settings · Zecamo" };

export default function AISettingsPage() {
  return <AISettingsView />;
}
